<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorAccent"
    android:weightSum="1">

    <ImageView
        android:id="@+id/requestlist_item_thumbnail"
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:src="@drawable/ic_user"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:id="@+id/requestlist_item_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/requestlist_item_thumbnail"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="13dp"
        android:layout_marginStart="13dp"
        android:layout_toEndOf="@+id/requestlist_item_thumbnail"
        android:layout_toRightOf="@+id/requestlist_item_thumbnail"
        android:text="TextView"
        android:textSize="18sp" />

    <ImageButton
        android:id="@+id/requestlist_item_button_accept"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/requestlist_item_thumbnail"
        android:layout_alignParentEnd="true"
        android:src="@android:drawable/ic_input_add" />

    <ImageButton
        android:id="@+id/requestlist_item_button_decline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/requestlist_item_button_accept"
        android:layout_toStartOf="@+id/requestlist_item_button_accept"
        android:src="@android:drawable/ic_delete" />

</RelativeLayout>